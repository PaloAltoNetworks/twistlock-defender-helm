{{- if .Values.image_pull_secret }}
{{- if and .Values.secret_store.name .Values.external_secrets.remote_key }}
apiVersion: {{ .Values.external_secrets.api_version }}
kind: ExternalSecret
metadata:
  name: {{ .Values.image_pull_secret }}
  namespace: {{ .Release.Namespace }}
  annotations:
    helm.sh/release-name: "{{ .Release.Name }}"
    helm.sh/release-namespace: "{{ .Release.Namespace }}"
    helm.sh/release-revision: "{{ .Release.Revision }}"
spec:
  refreshPolicy: {{ .Values.external_secrets.refresh_policy }}
  refreshInterval: {{ .Values.external_secrets.refresh_interval }}
  secretStoreRef:
    name: {{ .Values.secret_store.name }}
    kind: {{ .Values.secret_store.kind }}
  target:
    name: {{ .Values.image_pull_secret }}
    creationPolicy: Owner
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: "{\"auths\":{\"{{ "{{" }} .registry | lower {{ "}}" }}\":{\"username\":\"{{ "{{" }} .username {{ "}}" }}\",\"password\":\"{{ "{{" }} .password {{ "}}" }}\"}}}"
  data:
  - secretKey: registry
    remoteRef:
      key: {{ .Values.external_secrets.remote_key }}
      property: {{ .Values.external_secrets.properties_name.registry }}
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
  - secretKey: username
    remoteRef:
      key: {{ .Values.external_secrets.remote_key }}
      property: {{ .Values.external_secrets.properties_name.username }}
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
  - secretKey: password
    remoteRef:
      key: {{ .Values.external_secrets.remote_key }}
      property: {{ .Values.external_secrets.properties_name.password }}
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
{{- else if or (.Values.registry.dockerconfigjson) (and .Values.registry.name .Values.registry.username .Values.registry.password) }}
apiVersion: v1
data:
  {{- if .Values.registry.dockerconfigjson }}
  .dockerconfigjson: {{ .Values.registry.dockerconfigjson }}
  {{- else }}
  .dockerconfigjson: {{printf "{\"auths\": {\"%s\": {\"auth\": \"%s\"}}}" .Values.registry.name (printf "%s:%s" .Values.registry.username .Values.registry.password | b64enc) | b64enc }}
  {{- end }}
kind: Secret
metadata:
  name: {{ .Values.image_pull_secret }}
  namespace: {{ .Release.Namespace }}
type: kubernetes.io/dockerconfigjson
{{- end }}
{{- end }}
